<div class="container">
<h3>City Index</h3>
</div>
<div class="section hero">
	<div class="container">
	<ul style="list-style: none;">
	<% @cities.each do | city | %>
		<li>
			<a href="/cities/<%= city.id %>">
		<%= city.name %></a>
		</li>
	<% end %>
	</ul>
</div>
</div>

